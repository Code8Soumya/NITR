cd backend/tab1-social/

npm ci --omit=dev

Get-ChildItem -Exclude docs,scripts,deploy.zip,.env*,.git* | Compress-Archive -DestinationPath deploy.zip -Force

aws lambda update-function-code --function-name NITRHUB --zip-file fileb://deploy.zip --region ap-south-1

Remove-Item deploy.zip


Remove-Item Env:\AWS_ACCESS_KEY_ID -ErrorAction SilentlyContinue
Remove-Item Env:\AWS_SECRET_ACCESS_KEY -ErrorAction SilentlyContinue
Remove-Item Env:\AWS_SESSION_TOKEN -ErrorAction SilentlyContinue


psql -h database-1.cluster-cfm36uyk7ctc.ap-south-1.rds.amazonaws.com -U nitr_hub -d postgres -f 001_tab1_social.sql

psql -h database-1.cluster-cfm36uyk7ctc.ap-south-1.rds.amazonaws.com -U nitr_hub -d postgres


npx expo start -c
